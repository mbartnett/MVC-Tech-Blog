        <section class="dashboard-content">
            <div class="db-title">
                <span>{{post.title}}</span>
            </div>
            <div class="db-date">
                <span>{{format_date post.date_created}}</span>
            </div>
        </section>